You can install both ts-node and the official typescript package globally by running:
npm install --location=global ts-node typescript
npm uninstall -g ts-node typescript I decided uninstall

npm init
npm install --save-dev ts-node typescript

"scripts": {
    "ts-node": "ts-node"
  },

  npm run ts-node file.ts -- -s --someoption

  npm install --save-dev @types/node

  ============================

  {
  "compilerOptions": {
    "target": "ES2022",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitAny": true,
    "esModuleInterop": true,
    "moduleResolution": "node"
  }
}


  npm install express

  npm install @types/express --save-dev

  npm install --save-dev ts-node-dev (reload)

  npm install --save-dev eslint @eslint/js @types/eslint__js typescript typescript-eslint (about explicity Any type)
  file: eslint.config.mjs
  script: "lint": "eslint ."

npm install --save-dev @stylistic/eslint-plugin
